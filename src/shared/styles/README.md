# Shared Sass Architecture

All styling uses the `.sass` indented syntax.

Layers:
- `tokens/` for design tokens
- `mixins/` for reusable styling logic
- `animations/` for motion tokens and keyframes
- `themes/` for light and dark semantic theme values
- `reset/` and `base/` for global foundation
